{
  "scripts": {
    "dev": "docker compose -f docker-compose.yml -f docker-compose.dev.yml up",
    "dev:build": "docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build",
    "dev:mobile": "bash scripts/set-ip.sh && docker compose -f docker-compose.yml -f docker-compose.dev.yml up",
    "dev:mobile:build": "bash scripts/set-ip.sh && docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build",
    "prod": "docker compose up --build",
    "down": "docker compose down",
    "seed": "docker compose exec server node src/scripts/seed.js",
    "set-ip": "bash scripts/set-ip.sh"
  },
  "dependencies": {
    "sharp": "^0.34.5"
  }
}
